import 'package:flutter/material.dart';

class DietRecommendationPage extends StatelessWidget {
  const DietRecommendationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Diet Recommendation Screen"),
    );
  }
}
